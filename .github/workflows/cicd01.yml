on:
  push:
    branches:
      - main

jobs:
  My-Deploy-Job:
    runs-on: ubuntu-latest

    steps:
      - name: Hello World 출력
        run: echo "Hello, World!"

      - name: 여러개 명령어 추가하기
        run: |
          echo "Good"
          echo "Morning"

      - name: Github Actions 내장 변수 사용
        run: |
          echo $GITHUB_REPOSITORY
          echo $GITHUB_SHA